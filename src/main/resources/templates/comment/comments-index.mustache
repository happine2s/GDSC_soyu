{{! Comments }}
<br/><br/>
<div class="card">
    <div class="card-header bi bi-chat-dots"> {{#comments.size}}{{comments.size}}{{/comments.size}} Comments</div>
    {{! 댓글내용 부분 }}
    <ul class="list-group-flush">
        {{#comments}}
            <li id="comment-list-group" class="list-group-item">
                <span>
                    <span style="font-size: small">{{userName}}</span>
                    <!--<button class="btn btn-primary"> 수정</button>-->
                    <input type="hidden" id="postsId" value="{{posts.id}}">
                    <input type="hidden" id="commentId" value="{{id}}">
                    <button class="btn btn-danger btn-comment-delete" id="btn-comment-delete" value="{{id}}" onclick="main.commentDelete({{id}})"> 삭제</button>
                </span>
                <div id="comment-content">{{content}}</div>
            </li>
        {{/comments}}
    </ul>
</div>
